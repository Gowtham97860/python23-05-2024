n = int(input("Enter how many values you want to read : "))
a = []
for i in range(n):
    val = int(input(f"Enter the value of a[{i}] : "))
    a.append(val)
sum_val = sum(a)
print(f"The sum of array elements = {sum_val}")
